<p id="notice"><%= notice %></p>



<h1>These Are The Things You Can Buy</h1>

<table class="table">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= image_tag("#{product.id}.jpeg", size: '50x50') %></td>
        <td><%= product.title %></td>
        <td><%= product.description %></td>
        <td><%= product.price %></td>
        <td><%= link_to 'View Thing', product, class:'btn btn-sm btn-success' %></td>

        <% if current_user  %>
            <% if current_user.admin  %>
                <td><%= link_to 'Edit', edit_product_path(product) %></td>
                <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            <% end  %>
        <% end  %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% if current_user  %>
    <% if current_user.admin  %>
        <%= link_to 'New Product', new_product_path %>
    <% end  %>
<% end  %>